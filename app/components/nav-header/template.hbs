<header>
  <nav>
    <div class="nav-wrapper">
      <div class="settings-container {{if showSide 'change'}}" {{action 'toggleSettingsIcon'}}>
        <div class="bar1"></div>
        <div class="bar2"></div>
        <div class="bar3"></div>
      </div>
      <a href=#>Foodr</a>
      <a class='user'>{{user.name}}</a>
    </div>
    <div id='side-nav-id' class='side-nav {{if showSide 'change'}}'>
      {{link-to 'Home' 'index'}}
      {{link-to 'Restaurants' 'restaurants'}}
    </div>
  </nav>
</header>
{{yield}}