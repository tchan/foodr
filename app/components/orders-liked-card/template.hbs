{{#base-card title='Food I like'}}
  <div>{{input value=name placeholder='Add a new food'}} <span {{action 'addFood'}}>+</span></div>
  <ul>
  {{#each orders as |order|}}
    <li>{{order.name}}</li>
  {{/each}}
  </ul>
{{/base-card}}